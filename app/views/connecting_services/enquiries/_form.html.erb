<%= form_for(connecting_services_enquiry) do |f| %>
  <% if connecting_services_enquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(connecting_services_enquiry.errors.count, "error") %> prohibited this connecting_services_enquiry from being saved:</h2>

      <ul>
      <% connecting_services_enquiry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
