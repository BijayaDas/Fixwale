<p id="notice"><%= notice %></p>

<h1>Connecting Services Enquiries</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @connecting_services_enquiries.each do |connecting_services_enquiry| %>
      <tr>
        <td><%= link_to 'Show', connecting_services_enquiry %></td>
        <td><%= link_to 'Edit', edit_connecting_services_enquiry_path(connecting_services_enquiry) %></td>
        <td><%= link_to 'Destroy', connecting_services_enquiry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Connecting Services Enquiry', new_connecting_services_enquiry_path %>
