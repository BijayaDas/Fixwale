<%= form_for(full_time_employment) do |f| %>
  <% if full_time_employment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(full_time_employment.errors.count, "error") %> prohibited this full_time_employment from being saved:</h2>

      <ul>
      <% full_time_employment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
