<%= form_for(government) do |f| %>
  <% if government.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(government.errors.count, "error") %> prohibited this government from being saved:</h2>

      <ul>
      <% government.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
