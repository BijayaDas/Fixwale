<!-- START CONTAINER -->
<div class="page-container row-fluid container-fluid">
  <!-- SIDEBAR - START -->
  <%= render 'layouts/sidebar' %>
  <!--  SIDEBAR - END -->
  <!-- START CONTENT -->
  <section id="main-content" class=" ">
    <section class="wrapper main-wrapper row" style=''>
      <div class='col-xs-12'>
        <div class="page-title">
          <div class="pull-left">
            <h1 class="title">Skills</h1> </div>
          <div class="pull-right hidden-xs">
            <ol class="breadcrumb">
              <li>
                <a href="index.html"><i class="fa fa-home"></i>Home</a>
              </li>
              <li>
                <a href="crm-leads.html">Skills</a>
              </li>
              <li class="active">
                <strong>All Skills</strong>
              </li>
            </ol>
          </div>
        </div>
      </div>
      <div class="clearfix"></div>
      <div class="col-lg-12">
        <section class="box ">
          <header class="panel_header">
            <h2 class="title pull-left">All Skills</h2>
            <div class="panel_actions pull-right">
              <%= link_to '<i class="fa fa-plus"></i> Add Skill'.html_safe, new_skill_path, class: "btn btn-success" %>
            </div>
          </header>
          <div class="content-body">
            <div class="row">
              <div class="col-xs-12">
                <!-- ********************************************** -->
                <table id="example" class="display table table-hover table-condensed">
                  <thead>
                    <tr>
                      <th>ID</th>
                      <th>AVATAR</th>
                      <th>TITLE</th>
                      <th>DESCRIPTION</th>
                      <th>PARENT</th>
                      <th>ACTIONS</th>
                    </tr>
                  </thead>
                  <tbody>
                  <% @skills.each do |skill| %>
                    <tr>
                      <td><%= skill.id %></td>
                      <td class=" col-xs-1"><%= image_tag(skill.avatar, class: "img-rounded img-responsive") rescue ''%></td>
                      <td><%= skill.title %></td>
                      <td><%= skill.description %></td>
                      <td><%= skill.hash_tag rescue "NA" %></td>
                      <td>
                        <%= link_to 'Show', skill, class: "btn btn-info" %> 
                        <%= link_to 'Edit', edit_skill_path(skill), class: "btn btn-primary" %> 
                        <%= link_to 'Destroy', skill, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
                      </td>
                    </tr>
                  <% end %>

                  </tbody>
                </table>
                <!-- ********************************************** -->
              </div>
            </div>
          </div>
        </section>
      </div>
    </section>
  </section>
  <!-- END CONTENT -->
</div>
